# Cloudflare Pages frontend environment variables (example)
# Only public-safe values should be put here.

# Frontend -> backend API
PUBLIC_API_BASE_URL=https://api.example.com

# Cognito (public client settings for browser redirect flow)
COGNITO_DOMAIN = "https://us-west-1lmykvd7p8.auth.us-west-1.amazoncognito.com"
COGNITO_CLIENT_ID = "utqsaifm4gt707delac0g54um"
COGNITO_REDIRECT_URI = ""
COGNITO_SCOPE = "openid email profile phone"

# Optional frontend app metadata
PUBLIC_APP_ENV=production
